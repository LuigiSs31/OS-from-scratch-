
# --- Secrets & Environment ---
.env
.env.local
.env.development
.env.production
secrets.json
config.json

# --- Compiled Code & Artifacts ---
# Ignore files generated by compilers, assemblers, or build scripts.
# Your source code (.s, .c, .py) is what matters.
/build/
/dist/
/target/
/bin/
*.out
*.o
*.a
*.lib
*.so
*.dll
*.exe
*.bin

# Ignore the temporary source folder if it was created inside the repo
src_toolchain/

# Ignore system artifacts
.DS_Store

# --- Dependency Directories ---
# These are managed by package managers (like npm, pip, composer).
# Committing these inflates the repo; others can install them.
/node_modules/
/vendor/
/venv/
/.venv/

# --- System-Specific Files ---
# Ignore junk files created by different operating systems.
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Log & Cache Files ---
# Ignore temporary files, logs, and caches.
*.log
npm-debug.log*
yarn-error.log
/coverage/
.__pycache__/
.mypy_cache/
.pytest_cache/

# --- IDE & Editor Config ---
# Ignore project files from common editors.
# (Note: Some people put these in a 'global' .gitignore instead)
.idea/
.vscode/
*.suo
*.user

